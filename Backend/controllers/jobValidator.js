// const authenticate = (req, res, next) => {
//   const { requiredSkills } = req.body;
//   const skillsArray = requiredSkills.split(",").map((skill) => skill.trim());
//   req.body.requiredSkills = skillsArray;
//   next();
// };

// module.exports = authenticate;
